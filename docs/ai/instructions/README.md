# AI Instructions Index

## Canonical instructions
Use `.github/copilot-instructions.md` as the master instruction set.

## Legacy/reference set
The folder `docs/ai/instructions/beast-mode-v2/` is retained as reference context and historical guidance.

## Recovered originals
The full original instruction texts are preserved in:
- `docs/ai/instructions/legacy-originals/`

This folder contains recovered copies extracted from source bundles so no instruction content is lost.

## Conflict rule
If any file in `beast-mode-v2` conflicts with `.github/copilot-instructions.md`, follow `.github/copilot-instructions.md`.

## Why this exists
The repository previously contained overlapping instruction files. This index defines a single authoritative source to reduce ambiguity for contributors and coding agents.
